import { TeamScoreFilterPipe } from './team-score-filter.pipe';

describe('TeamScoreFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TeamScoreFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
