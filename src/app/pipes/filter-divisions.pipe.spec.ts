import { FilterDivisionsPipe } from './filter-divisions.pipe';

describe('FilterDivisionsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDivisionsPipe();
    expect(pipe).toBeTruthy();
  });
});
