import { PlayerCardPipe } from './player-card.pipe';

describe('PlayerCardPipe', () => {
  it('create an instance', () => {
    const pipe = new PlayerCardPipe();
    expect(pipe).toBeTruthy();
  });
});
